meta {
  name: Get Entitlement by ID
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/plans/{{planId}}/entitlements/{{entitlementId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}