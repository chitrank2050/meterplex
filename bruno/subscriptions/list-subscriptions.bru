meta {
  name: List All Subscriptions (History)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/subscriptions
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}