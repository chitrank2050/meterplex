meta {
  name: Get Active Subscription
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/subscriptions/active
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}