meta {
  name: Cancel Subscription
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/{{apiPrefix}}/v1/subscriptions/{{subscriptionId}}/cancel
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}