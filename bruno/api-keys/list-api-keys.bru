meta {
  name: List API Keys
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/api-keys
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}