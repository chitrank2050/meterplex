meta {
  name: Mark Invoice Paid
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/{{apiPrefix}}/v1/invoices/{{invoiceId}}/mark-paid
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}