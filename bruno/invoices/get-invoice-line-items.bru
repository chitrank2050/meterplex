meta {
  name: Get Invoice Line Items
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/invoices/{{invoiceId}}/line-items
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}