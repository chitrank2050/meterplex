meta {
  name: Finalize Invoice
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/{{apiPrefix}}/v1/invoices/{{invoiceId}}/finalize
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}