meta {
  name: Get Invoice by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/invoices/{{invoiceId}}
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}