meta {
  name: Logout
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/{{apiPrefix}}/v1/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "refreshToken": "{{refreshToken}}"
  }
}