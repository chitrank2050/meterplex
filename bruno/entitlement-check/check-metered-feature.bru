meta {
  name: Check Feature (METERED - storage)
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/entitlements/storage/check
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}