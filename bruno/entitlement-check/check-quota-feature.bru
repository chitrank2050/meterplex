meta {
  name: Check Feature (QUOTA - api_calls)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/entitlements/api_calls/check
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}