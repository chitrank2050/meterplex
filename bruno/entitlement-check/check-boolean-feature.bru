meta {
  name: Check Feature (BOOLEAN - sso)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/{{apiPrefix}}/v1/entitlements/sso/check
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  x-tenant-id: {{tenantId}}
}